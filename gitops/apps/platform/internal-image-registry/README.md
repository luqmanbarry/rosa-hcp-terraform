# internal-image-registry

Configures the OpenShift internal image registry.

## Default

- internal route disabled
- redirect disabled
- two replicas

## Notes

Keep the route disabled unless there is a clear requirement to expose the registry externally.
