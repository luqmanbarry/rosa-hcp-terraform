# groups-rbac

Creates cluster role bindings for enterprise groups.

## Use

Provide assignments in the cluster values file:

- group name
- OpenShift cluster role

The chart is intentionally a no-op until assignments are provided.
