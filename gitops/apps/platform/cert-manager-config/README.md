# Cert Manager Config

Configures cert-manager resources after the cert-manager operator is installed.

Use this module for shared `ClusterIssuer` objects.

Keep it disabled until you define at least one real issuer.
