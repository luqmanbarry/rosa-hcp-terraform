# Playbooks

This directory contains Ansible Automation Platform examples for running the ROSA HCP factory from automation.

- `aap-run-factory.yml`: run validate, plan, or apply for one cluster by calling the shared workflow script

Use [aap-execution.example.yml](../docs/operations/aap-execution.example.yml) as the starting point for AAP extra vars.
